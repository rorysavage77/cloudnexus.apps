apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-monitoring
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: k8s-monitoring
    targetRevision: 3.8.0
    helm:
      values: |
        cluster:
          name: starbase

        # --- Aggressive Fix for kube-state-metrics ---
        # Increases resources to move out of BestEffort QoS and relaxes probes
        kube-state-metrics:
          enabled: true
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
          extraArgs:
            - --use-discovery-endpoint-slice=true
            # Disabling the deprecated endpoints collector causing the log flood
            - --resources=-endpoints 

        # --- Fix for Alloy components ---
        # Provides resource guarantees to match the high restart counts observed
        alloy-operator:
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi

        # Features
        clusterMetrics:
          enabled: true
        clusterEvents:
          enabled: true
        podLogs:
          enabled: true

        # Explicitly enabling the Alloy collectors
        alloy-metrics:
          enabled: true
        alloy-singleton:
          enabled: true
        alloy-logs:
          enabled: true

        destinations:
          - name: loki-destination
            type: loki
            url: http://loki-gateway.monitoring.svc.cluster.local/loki/api/v1/push
          - name: local-prometheus
            type: prometheus
            url: http://monitoring-stack-prometheus.monitoring.svc.cluster.local:9090/api/v1/write

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
